//import 'package:flutter/material.dart';

//import 'Game5.dart';
/*
class Game5HomePage extends StatelessWidget {
  const Game5HomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Game 5'),
      ),
      body: Center(
        child: Column(
          mainAxisAlignment: MainAxisAlignment.center,
          children: [
            const Text(
              'Bienvenue dans notre jeu tic tac tie\n'
                  'le jeu consiste à  ',
              style: TextStyle(fontSize: 20),
            ),
            SizedBox(height: 20),
            //ElevatedButton(
              //onPressed: () {
                //Navigator.push(
                    //context,
                    //PageRouteBuilder(
                      //  pageBuilder: (_, __, ___) => Game5()
                   // )
                //);
              //},
              //child: Text('Jouer'),
              //style: ElevatedButton.styleFrom(
               // fixedSize: Size(100, 30),
               // backgroundColor: Colors.purpleAccent,
              ),
            ),
          ],
        ),
      ),
    );
  }
}
*/